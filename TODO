TODO LOW_PRIO
Implement zlib support to enable gziped data transfers between nodes.
Support of dirtee synchronization instead of single directory (depth 1)
Implement filters

TODO HIGH_PRIO
Implemment logging function
Clean up the Install mess (finish the Makefile install process, find a way to change defines at configure time, to handle location prefixes )
Finish Documentation

